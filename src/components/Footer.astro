<footer id="contato">
  <div class="footer-container">
    <!-- Lado esquerdo -->
    <div class="footer-info text-center">
      <h2>Entre em contato!</h2>
      <p>Fale diretamente comigo para dúvidas, projetos ou parcerias.</p>

      <div class="contact-links">
        <a
          href="https://www.linkedin.com/in/marcio-alvim"
          target="_blank"
          rel="noopener"
        >
          <i class="bi bi-linkedin"></i>
        </a>
        <a href="https://github.com/marcio-web" target="_blank" rel="noopener">
          <i class="bi bi-github"></i>
        </a>
        <a href="https://wa.me/5531991248291" target="_blank" rel="noopener">
          <i class="bi bi-whatsapp"></i>
        </a>
      </div>
    </div>

    <!-- Lado direito -->
    <div class="footer-form">
      <form id="contact-form">
        <input type="text" name="name" placeholder="Seu nome" required />
        <input
          type="email"
          name="email"
          placeholder="Seu email"
          required
        />
        <input type="text" name="title" placeholder="Assunto" required />
        <textarea name="message" placeholder="Sua mensagem" required></textarea>
        <button type="submit">Enviar</button>
        <span id="form-status"></span>
      </form>
    </div>
  </div>
</footer>

<style>
  #contato {
    background: var(--color-primary);
    color: #fff;
    padding: 4rem 1rem;
  }

  .footer-container {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .footer-info {
    flex: 1;
  }

  .footer-info h2 {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }

  .footer-info p {
    margin-bottom: 1.5rem;
    color: rgba(255, 255, 255, 0.9);
  }

  .contact-links {
    display: flex;
    justify-content: center;
    gap: 2rem;
  }

  .contact-links a {
    color: #fff;
    font-size: 1.7rem;
    transition: color 0.2s ease;
  }

  .contact-links a:hover {
    color: #ffd700;
  }

  /* Formulário */
  .footer-form {
    flex: 1;
  }

  #contact-form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  #contact-form input,
  #contact-form textarea {
    padding: 0.8rem;
    border-radius: 8px;
    border: none;
    font-size: 1rem;
  }

  #contact-form button {
    padding: 0.8rem;
    border-radius: 8px;
    border: none;
    background: #fff;
    color: var(--color-primary);
    font-weight: 600;
    cursor: pointer;
    transition: transform 0.2s ease;
  }

  #contact-form button:hover {
    transform: scale(1.05);
  }

  /* Responsivo */
  @media (max-width: 900px) {
    .footer-container {
      flex-direction: column;
      align-items: center;
      text-align: center;
    }

    .footer-form,
    .footer-info {
      width: 100%;
    }

    .contact-links {
      justify-content: center;
    }

    #contact-form {
      padding: 0 15%;
    }
  }
</style>

<script>
  import emailjs from "emailjs-com";

  const form = document.getElementById("contact-form")! as HTMLFormElement;
  const status = document.getElementById("form-status")!;

  form.addEventListener("submit", function (e) {
    e.preventDefault();

    emailjs
      .sendForm("service_ubn2jog", "template_tn3kfg8", this, "UpbXFI4GFm0vNKIqD")
      .then(
        () => {
          status.textContent = "Mensagem enviada com sucesso!";
          form.reset();
        },
        (err: unknown) => {
          status.textContent = "Erro ao enviar, tente novamente.";
          console.error(err);
        }
      );
  });
</script>
